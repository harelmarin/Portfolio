var options = {
    strings: ["étudiant", "développeur", "designer", "passioné"],
    typeSpeed: 100,
    backSpeed: 50,
    backDelay: 2000,
    loop: true
  };
  
  var typed = new Typed(".dynamic-text", options);